[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Proxy the admin dashboard (a separate Netlify site/repo) under /admin/ so the
# browser sees it as this same origin. The dashboard<->site test bridge
# (cms-bridge.js) uses localStorage, which is scoped per-origin — without this,
# the two deployed sites can never share data no matter what browser/device is
# used. Status 200 = proxy/rewrite (content served, URL stays /admin...), not
# a redirect — both rules below proxy directly, with no 301 step between them,
# since chaining an internal redirect into the proxy rule caused a redirect
# loop (Netlify's own trailing-slash handling likely double-matched it).
# Swap this out once the real Firestore backend replaces the bridge.
[[redirects]]
  from = "/admin"
  to = "https://gogohankukdashboard.netlify.app/"
  status = 200
  force = true

[[redirects]]
  from = "/admin/*"
  to = "https://gogohankukdashboard.netlify.app/:splat"
  status = 200
  force = true
